import { ArraySorteerPipe } from './array-sorteer.pipe';

describe('ArraySorteerPipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySorteerPipe();
    expect(pipe).toBeTruthy();
  });
});
