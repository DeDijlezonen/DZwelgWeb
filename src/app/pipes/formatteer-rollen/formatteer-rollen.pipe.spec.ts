import { FormatteerRollenPipe } from './formatteer-rollen.pipe';

describe('FormatteerRollenPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatteerRollenPipe();
    expect(pipe).toBeTruthy();
  });
});
