import { InschrijvingsFilterPipe } from './filter.pipe';

describe('InschrijvingsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InschrijvingsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
